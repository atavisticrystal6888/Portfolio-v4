# Plan: portfolio polish pass (17-item checklist)

Date: 2026-09-03. Planned by Fable 5.1 from a four-agent read-only audit. Execution, optimisation and coding by Opus 5 agents.
Branch: `fix/site-polish-2026-09` (from `main` at 89c4d45). Stage by explicit path only. Do not stage the pre-existing uncommitted files (README.md, .gitignore, content/github-profile-README.md, docs/hero-*.png, plan-*.md, research-*.md) — they belong to another session.

## 1. Checklist vs audit baseline

| # | Checklist item | Baseline finding | Verdict |
|---|---|---|---|
| 1 | Horizontal scroll | html `overflow-x: hidden`, body `overflow-x: clip` (src/styles/base.css:311-318). `tests/e2e/responsive.spec.ts` asserts scrollWidth ≤ clientWidth at 320/375px on 5 routes. Intentional `overflow-x: auto` on `<pre>`, case-study tables, product carousel. | Verify by running tests; widen route coverage |
| 2 | Meta descriptions | Every route has one via `generatePageMetadata` (src/lib/metadata.ts). Blog/case-study pages use `excerpt`/`tldr` untruncated. | Audit lengths (≤160 chars) and uniqueness |
| 3 | Favicon | `src/app/favicon.ico` is the 25,931-byte **default Next.js triangle** from the initial commit. `icon.tsx` / `apple-icon.tsx` / `public/icon.svg` render the branded "DS" mark. `layout.tsx:41` lists `/favicon.ico` as shortcut. | **Gap: replace favicon.ico with the branded mark** |
| 4 | Page titles | Root template `%s \| Dhruv Singhal`; home uses `{ absolute }`; 404 titled. | Verify rendered `<title>` per route, no duplicates |
| 5 | Compress images | 25 files / 4.8 MB in public/images. 9 PNGs > 200 KB (aarchid/pricing 788 KB, platform 556 KB, hero 536 KB, desktasks/hero 300 KB, portfolio-site/about 280 KB, kite-edge/quantstats-tearsheet 240 KB, experiment-hub/dashboard 216 KB, docs/hero-dark 352 KB, docs/hero-light 308 KB). `next/image` serves AVIF/WebP for `<Image>` uses; Lightbox.tsx:145 and ZoomImage.tsx:51 use raw `<img>` and ship the originals. `sharp` is installed; no image script exists. | **Gap: compress originals in place** |
| 6 | Clickable email | `CONTACT_EMAIL_HREF` mailto in DirectLinks.tsx:8 (with copy-to-clipboard), bookshelf/page.tsx:145, home CTA. | Done; add aria-live to "Copied!" |
| 7 | Fix broken links | All internal hrefs resolve. `tests/e2e/links.spec.ts` exists. External URLs not checked live. | Verify externals live |
| 8 | Add mobile menu | Navbar.tsx hamburger 44×44 + MobileNav.tsx drawer: focus trap, Escape, route-change close, scroll lock, aria-expanded/aria-modal. | Done; verify in mobile Playwright projects |
| 9 | Remove placeholder text | Grep for lorem/TODO/TBD/placeholder/coming soon/draft across src/ and content/: none. | Done |
| 10 | Test on a mobile device | Playwright projects `mobile-chrome` (Pixel 5) and `mobile-safari` (iPhone 13) exist. | Run against a production build; hand user a real-device checklist |
| 11 | Empty state page | ProjectGrid.tsx:132 plain `<p>`; BlogSearch.tsx:70-80 text only; CommandPalette "No results found". No shared component, no recovery action. | **Gap: shared EmptyState with a clear-filter action** |
| 12 | Optimise for mobile | Breakpoints at 480–1200px across components; all tap targets ≥ 44px (base.css:349). No explicit `viewport` export in layout.tsx (Next default applies). | Add explicit viewport export |
| 13 | Fix mobile overflow | CostModelDemo.module.css:13 `minmax(200px, 1fr)` `.controls` has no override under 600px. | **Gap: single-column rule** |
| 14 | Add error messages | ContactForm: inline field errors, `role="alert"`, server errors surfaced, mailto fallback. **No `src/app/error.tsx` or `global-error.tsx`** — a render error shows Next's bare default. `api/contact/route.ts` returns success when `RESEND_API_KEY` is absent (silently drops mail). | **Gap: error boundaries + honest API failure** |
| 15 | Add success messages | ContactForm `role="status"` success + form clear; DirectLinks "Copied!". | Done; add aria-live on copy state |
| 16 | Add 404 page | `src/app/not-found.tsx` with suggested links and title. `not-found-and-theme.spec.ts` covers it. | Done |
| 17 | Clickable phone number | Phone `+91 7015131885` exists only in content/resume/*.md (PDF script already emits `tel:`). Not rendered anywhere on the site. `formatDetection.telephone: false` in layout (fine with explicit tel: links). | **Gap: add CONTACT_PHONE + tel: link** (publishing decision: the number is already in the public repo's resume markdown) |

## 2. Action tracker

Priority: P1 = user-visible gap, P2 = quality/perf, P3 = hygiene. Source: AU = audit finding, CL = checklist item with no current gap (verify only).

| ID | Area | Action item | Where | Current state | Pri | Src | Owner | Status |
|---|---|---|---|---|---|---|---|---|
| 1.1 | SEO | Replace default Next.js favicon.ico with the branded "DS" mark (multi-size ICO: 16/32/48) generated by script from the same gradient/typography as icon.tsx | src/app/favicon.ico, new scripts/generate-favicon.mjs | Vercel triangle ships today | P1 | AU | Opus W1-A | Done |
| 1.2 | SEO | Explicit `viewport` export (width=device-width, initialScale 1, no maximumScale/userScalable restriction) | src/app/layout.tsx | Implicit default | P3 | AU | Opus W1-A | Done |
| 1.3 | SEO | Audit every route's rendered description: 50–160 chars, unique, no trailing markdown; truncate blog `excerpt` / case-study `tldr` at a word boundary with "…" inside generateMetadata | src/app/blog/[slug]/page.tsx, src/app/projects/[slug]/page.tsx, src/lib/metadata.ts | Untruncated | P2 | CL | Opus W1-A | Done |
| 1.4 | SEO | Assert rendered `<title>` per route: matches `X \| Dhruv Singhal`, home is absolute, no double suffix; extend tests/e2e/seo.spec.ts with description-length check | tests/e2e/seo.spec.ts | Partial coverage | P2 | CL | Opus W1-A | Done |
| 2.1 | Images | `scripts/optimize-images.mjs` (sharp): for every PNG/JPG under public/images and docs/, re-encode (PNG: palette quantisation, compressionLevel 9, effort 10; JPEG: mozjpeg q82) and overwrite only when ≥ 15 % smaller; print before/after table. Keep filenames and dimensions so no references change | new scripts/optimize-images.mjs, package.json `images:optimize` | 4.8 MB, 9 files > 200 KB | P1 | CL | Opus W1-B | Done |
| 2.2 | Images | Run the script; target every file ≤ 300 KB, total public/images ≤ 2.5 MB; visually spot-check the three aarchid screens are not banded | public/images/**, docs/hero-*.png | — | P1 | CL | Opus W1-B | Done |
| 2.3 | Images | Raw `<img>` in Lightbox.tsx:145 and ZoomImage.tsx:51: add `loading="lazy"` `decoding="async"` where missing; keep raw `<img>` (arbitrary sizes) | src/components/… | Ships originals eagerly | P3 | AU | Opus W1-B | Done |
| 3.1 | Contact | Add `CONTACT_PHONE = "+91 70151 31885"` and `CONTACT_PHONE_HREF = "tel:+917015131885"` to site config | src/lib/site.ts | Absent | P1 | CL | Opus W1-C | Done |
| 3.2 | Contact | Render phone as a `tel:` link in DirectLinks with the same copy-to-clipboard affordance as email; add `telephone` to Person JSON-LD and contact page schema | src/components/contact/DirectLinks.tsx, src/lib/metadata.ts, src/app/contact/page.tsx | Email only | P1 | CL | Opus W1-C | Done |
| 3.3 | Contact | "Copied!" state: wrap in `aria-live="polite"` so screen readers hear the success message | src/components/contact/DirectLinks.tsx:54 | Visual only | P2 | AU | Opus W1-C | Done |
| 3.4 | Contact | When `RESEND_API_KEY` is missing in production, return 503 `{ error: "Contact form is temporarily offline — email me directly." }` instead of 200; keep console fallback in development. ContactForm already shows server `error` with mailto fallback — confirm it renders this string | src/app/api/contact/route.ts, src/components/contact/ContactForm.tsx | Silent success | P1 | AU | Opus W1-C | Done |
| 3.5 | Contact | Honeypot field (`website`, visually hidden, tabIndex -1, autocomplete off); server rejects with 200-shaped silent drop when filled | ContactForm.tsx, route.ts | Rate limit only | P3 | AU | Opus W1-C | Done |
| 4.1 | Errors | Add `src/app/error.tsx` (client): heading, one-line plain-English message, "Try again" (calls `reset`) and "Go home" buttons, links to /projects and /contact; style matches not-found.tsx; logs `error.digest` | new src/app/error.tsx | Missing | P1 | CL | Opus W1-D | Done |
| 4.2 | Errors | Add `src/app/global-error.tsx` with its own `<html><body>` and inline styles (no CSS modules; root layout is gone at this point) | new src/app/global-error.tsx | Missing | P2 | CL | Opus W1-D | Done |
| 4.3 | Empty state | Shared `EmptyState` component (icon slot, title, description, optional action button) | new src/components/ui/EmptyState.tsx + module.css | None | P1 | CL | Opus W1-D | Done |
| 4.4 | Empty state | Use EmptyState in ProjectGrid (action: "Show all projects" resets filter) and BlogSearch (action: "Clear search"); keep CommandPalette inline text | src/components/projects/ProjectGrid.tsx:132, src/components/blog/BlogSearch.tsx:70 | Plain text | P1 | CL | Opus W1-D | Done |
| 4.5 | Errors | Unit tests (vitest + RTL): error.tsx renders and calls reset; EmptyState action fires | tests/unit/ | — | P2 | CL | Opus W1-D | Done |
| 5.1 | Mobile | `.controls { grid-template-columns: 1fr }` under 600px | src/components/ai-pm/CostModelDemo.module.css | minmax(200px) | P2 | AU | Opus W1-D | Done |
| 5.2 | Mobile | Extend responsive.spec.ts route list to include /ai-pm, /lab, /contact, one case study (/projects/aarchid) and one blog post at 320/360/375/414px | tests/e2e/responsive.spec.ts | 5 routes | P2 | CL | Opus W2 | Done |
| 5.3 | Mobile | Production build, then run `mobile-chrome` + `mobile-safari` Playwright projects and the accessibility suite; fix every failure at the source (no test loosening) | — | Unknown | P1 | CL | Opus W2 | Done |
| 5.4 | Mobile | Real-device checklist for the user (cannot be automated): URLs, gestures, what to look for; write to docs/mobile-device-checklist.md | docs/ | — | P2 | CL | Opus W2 | Done |
| 6.1 | Links | Live-check every external URL from the audit list (HEAD/GET, follow redirects, 10 s timeout); report status table; fix or remove dead ones (never re-add private repo links: kite-edge, desktasks, better-half, weekly-retro) | content/, src/ | Unchecked | P1 | CL | Opus W2 | Done |
| 6.2 | Links | Run tests/e2e/links.spec.ts against the build | — | — | P2 | CL | Opus W2 | Done |
| 7.1 | Verify | `npm run lint`, `npm run test`, `npm run build` clean; Lighthouse mobile on `/`, `/projects/aarchid`, `/contact` via lighthouserc.json, report performance/SEO/a11y scores before vs after | — | — | P1 | CL | Opus W2 | Done |
| 7.2 | Ship | One commit per workstream (explicit paths), then summary of what changed, what was verified, and what is left for the user (phone publication confirmed, real-device pass) | — | — | P1 | — | Opus W3 | Done |

## 3. Execution waves (Opus 5)

**Wave 1 — four parallel agents, disjoint files.**
- W1-A SEO: 1.1, 1.2, 1.3, 1.4. Touches src/app/layout.tsx, src/app/favicon.ico, scripts/generate-favicon.mjs, src/lib/metadata.ts, src/app/blog/[slug]/page.tsx, src/app/projects/[slug]/page.tsx, tests/e2e/seo.spec.ts.
- W1-B Images: 2.1, 2.2, 2.3. Touches scripts/optimize-images.mjs, package.json (scripts block only), public/images/**, docs/hero-*.png, Lightbox.tsx, ZoomImage.tsx.
- W1-C Contact: 3.1–3.5. Touches src/lib/site.ts, DirectLinks.tsx (+css), ContactForm.tsx, api/contact/route.ts, contact/page.tsx, src/lib/metadata.ts (Person schema `telephone` only — coordinate: W1-A owns the rest of that file; W1-C adds one field).
- W1-D Errors/empty/mobile CSS: 4.1–4.5, 5.1. Touches src/app/error.tsx, global-error.tsx, src/components/ui/EmptyState.*, ProjectGrid.tsx, BlogSearch.tsx, CostModelDemo.module.css, tests/unit/.

**Wave 2 — one agent, after wave 1:** 5.2, 5.3, 5.4, 6.1, 6.2, 7.1. Build, run, fix, report.

**Wave 3 — one agent:** 7.2 commits and final report.

## 4. Guardrails for executing agents
- No Artifact tool. Deliverables are files in this repo and the terminal report.
- Stage by explicit path. Never `git add -A` / `.` / `-a`. Do not touch the other session's uncommitted files listed at the top.
- Do not add links to private repos (kite-edge, desktasks, better-half, weekly-retro). Do not change project copy or metrics.
- Keep existing visual language (CSS modules, design tokens in src/styles). No new UI libraries.
- Report ≤ 1,500 tokens: what changed (file:line), what was verified with the command and its tail output, what failed.

## 5. Outcome

All 27 tracker rows are Done; nothing was deferred. Verified on branch `fix/site-polish-2026-09`.

- **Tests.** Playwright against a production build: **590 passed / 10 skipped** across `chromium`, `mobile-chrome` (Pixel 5) and `mobile-safari` (iPhone 13). Vitest: **75 passed** at the wave-2 gate, **76** after the DirectLinks tap fix added a case.
- **Lighthouse (mobile, throttled).**

  | Route | Perf | A11y | Best practices | SEO |
  |---|---|---|---|---|
  | `/` | 65 | 100 | 100 | 100 |
  | `/projects/aarchid` | 65–78 | 100 | 100 | 100 |
  | `/contact` | 75 | 100 | 100 | 100 |

- **Images.** `public/images` 4106 KB → **1426 KB** (−65 %) via `scripts/optimize-images.mjs`; filenames and dimensions unchanged, no reference edits needed.
- **Dead link.** Removed the Kite-edge repo URL from `content/resume/dhruv-singhal-ai-pm.md` (private repo, 404 in the live check).
- **DirectLinks tap fix.** The tile's `preventDefault()` copy-on-click made the Phone and Email tiles unusable on touch: the `mailto:`/`tel:` anchors now navigate, and copying moved to a separate 44px "Copy" button inside the tile but outside the anchor (checklist items 6, 15, 17).

**Open / not fixable here**

- Home performance is bounded by the three.js + framer-motion hero bundle — main-thread work is ~9 s under mobile throttling. Lifting `/` past ~65 means changing what the hero is, which is a product decision, not a polish task.
- The real-device pass is the user's: `docs/mobile-device-checklist.md` lists the URLs, gestures and things to look for on an actual phone.
